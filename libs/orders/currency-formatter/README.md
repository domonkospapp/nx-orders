# orders-currency-formatter

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test orders-currency-formatter` to execute the unit tests via [Jest](https://jestjs.io).
