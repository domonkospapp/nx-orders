import { ordersCurrencyFormatter } from './orders-currency-formatter';

describe('ordersCurrencyFormatter', () => {
  it('should work', () => {
    expect(ordersCurrencyFormatter()).toEqual('orders-currency-formatter');
  });
});
